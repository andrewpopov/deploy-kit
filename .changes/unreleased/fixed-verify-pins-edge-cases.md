---
kind: fixed
summary: verify-pins: tolerate absent optional/peer pins (mismatch still fails), verify semver:<exact> and build-metadata refs, compare versions by semver equality, and fail corrupt installs
---

Describe the user-facing change in one short paragraph before releasing.
